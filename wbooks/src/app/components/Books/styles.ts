import { StyleSheet } from 'react-native';

import { lithtBlueBackgroud } from '../../../constants/colors';

export const styles = StyleSheet.create({
  bookCardSeparator: {
    height: 10
  },
  container: {
    backgroundColor: lithtBlueBackgroud,
    padding: 15
  }
});
