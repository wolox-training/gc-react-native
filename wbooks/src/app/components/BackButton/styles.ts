import { StyleSheet } from 'react-native';

const imageSize = 25;

const styles = StyleSheet.create({
  imgage: {
    marginLeft: 15,
    height: imageSize,
    width: imageSize
  }
});

export default styles;
