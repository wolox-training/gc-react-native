import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardSeparator: {
    height: 20
  },
  viewAllButton: {
    borderWidth: 0,
    marginTop: 0
  },
  viewAllText: {
    fontWeight: 'normal'
  }
});

export default styles;
