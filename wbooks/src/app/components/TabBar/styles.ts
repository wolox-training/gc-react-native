import { StyleSheet } from 'react-native';
import { darkLightBlue, dark } from '../../../constants/colors';

const styles = StyleSheet.create({
  focused: {
    color: darkLightBlue
  },
  inactive: {
    color: dark
  }
});

export default styles;
